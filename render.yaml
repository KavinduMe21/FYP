services:
  - type: web
    name: rugby-knock-on-detector
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.ballDetect:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
